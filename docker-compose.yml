services:
  arcane-mcp:
    build: .
    container_name: arcane-mcp-server
    ports:
      - "8788:8788"
    environment:
      ARCANE_API_KEY: ${ARCANE_API_KEY}
      ARCANE_BASE_URL: ${ARCANE_BASE_URL}
      MCP_AUTH_TOKEN: ${MCP_AUTH_TOKEN}
    restart: unless-stopped
