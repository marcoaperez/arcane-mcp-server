# Balance de F5 — build y registries

- **Fecha:** 2026-08-19
- **Punto de partida:** `e72bbcf` — cierre de F4 (merge), 100 tools, 294 tests unitarios
  de línea base, 59 e2e. **Corrección de esta revisión (§2, §3.3):** la cifra de
  cobertura publicada en su momento como partida, «98 de 249», se copió del balance de
  F4 sin remedirla con el instrumento de F5. Remedida hoy con el mismo script AST que
  mide la llegada: **97**, no 98. La diferencia de uno es
  `VolumeBackupsMethods.download()`, que llama a `this.client.fetchFn(...)` directamente
  y el script de F5 no cuenta porque solo busca las cinco variantes de `request*` — ver
  §3.3 para la medición completa.
- **Punto de llegada:** `619040a` — 21 commits desde `e72bbcf` (19 del cierre
  original de F5 + los 2 de código/tests de esta revisión final), 24 ficheros,
  +6.707 / −51. Este balance se cierra en un commit posterior a `619040a` que solo
  toca documentación (este fichero y los otros tres corregidos en D2/D4); ese commit
  de documentación no mueve ninguna cifra de código. **Corrección de esta revisión
  (§3.3.1):** el rango publicado originalmente, `4eaf8b0..8b6eb12`, tenía dos errores:
  `4eaf8b0` no es el cierre de F4 —es ya el cuarto commit de F5— y el rango dejaba
  fuera el spec, el plan y `scripts/e2e-remoto.sh`, que este mismo balance discute en
  la §4 y la §9 del spec.
- **Estado:** implementación completa de 16 de las 17 tools previstas; la 17.ª
  (`arcane_build_workspace_upload`) queda diferida por decisión del propietario, no
  excluida. Ocho tareas revisadas una a una, más la revisión final de rama, cuyos
  hallazgos cierra este mismo commit. Suite unitaria y e2e verificadas en esta sesión.

---

## 1. Resumen en una frase

F5 añade 16 tools sobre registros de contenedor, registros de plantillas, el workspace
de builds y la construcción de imágenes/proyectos, deja la 17.ª diferida por una
comprobación e2e medida como imposible en cualquier entorno, y arrastra menos hallazgos
propios que fases anteriores: la mayoría de lo que salió mal esta vez venía **del propio
plan**, no de la implementación — un código de ejemplo que contradecía tres veces sus
propias restricciones, una afirmación falsa sobre el spec, y una mutación de
falsabilidad que no falsificaba nada.

## 2. Cifras medidas hoy

Todas salen de un comando ejecutado en esta sesión, sobre el árbol de
`f5-build-y-registries` al cierre de la revisión final de rama (hallazgos A1–D4). El
comando exacto de cada una está en la sección 3.

| Métrica | Comando | Resultado |
|---|---|---|
| Tests unitarios | `npm test` | **380 passed** (5 ficheros) — sube de 376: B1 (recorte de `output` + su ausencia en `_get` sin truncar) y A6 (aviso de `maxBytes`) añaden 4 tests nuevos |
| `tsc --noEmit` | `npm run type-check` | Limpio, sin salida |
| Tools registradas | `npm run gen-tools-table -- --check` | **116** (`OK: la tabla del README.md está al día`) — sin cambios: ninguno de los arreglos añade ni quita tools |
| Drift de campos | `node scripts/audit-schema-drift.mjs` | **24 desalineaciones** (sin cambios) |
| Tests e2e | `./scripts/e2e-remoto.sh`, líneas `✓` contadas a mano | **77 de 77, 0 skipped** (9 ficheros, 1 intento de script — con 1 reintento interno en el primer `ssh` de la copia, `Connection refused`, resuelto solo en el segundo) — sube de 76: B2 añade una aserción a un test ya existente (no cuenta) y B3 añade un `it()` nuevo (+1) |
| Cobertura método+ruta | script AST temporal (ver 3.3) | **113 de 347** en bruto; **113 de 244** sobre el denominador honesto (sin cambios: los arreglos de esta revisión no tocan `arcane-client.ts`) |
| Denominador honesto | comando de `criterio-exposicion.md` §5 | **244** (sin cambios) |

Partida verificada al arrancar F5 (`e72bbcf`, cierre de F4): 294 tests, 59 e2e, 100
tools, 21 desalineaciones, 97/249 operaciones (remedido con el instrumento de F5 en
esta revisión final; el balance de F4 publicó 98 con un instrumento propio, ya perdido
— ver §3.3).

## 3. Cómo se midió cada cifra

### 3.1 Verificación estática

```
npm test                                    → Test Files 5 passed (5); Tests 380 passed (380)
npm run type-check                          → sin salida
npm run gen-tools-table -- --check          → OK: la tabla del README.md está al día (116 tools).
node scripts/audit-schema-drift.mjs         → Total: 24 desalineaciones.
```

(376 → 380 es de esta revisión final, no del cierre original de F5: B1 añade dos tests
de `arcane_image_build_get` que demuestran el recorte de `output` por la cola, y A6
añade dos tests de `arcane_build_workspace_read` que demuestran el aviso de posible
truncado por `maxBytes`. Los cuatro, mutados y revertidos, ver §4 de este documento y
el commit de cierre.)

**El drift sube de 21 a 24 pese a que cinco de los siete tipos nuevos están alineados al
cien por cien.** Los siete tipos que entran en el `MAP` esta fase son
`ContainerRegistry`, `RegistryPullUsage`, `TemplateRegistry`, `BuildWorkspaceEntry`,
`BuildFileContent`, `ImageBuildRecord` y `UploadSession`. Solo dos producen hallazgos:

- `BuildWorkspaceEntry` marca `relativePath` y `editable` como
  `OPCIONAL-PERO-REQUERIDO`: el spec de Arcane los declara obligatorios y la instancia
  real no los devuelve siempre. **Es correcto y esperado — no se ha tocado.** El tipo
  sigue la realidad medida, no el spec, tal y como decidió la Tarea 5.
- `UploadSession` marca `INTERFAZ-AUSENTE`: no existe como interfaz TS porque la Tarea 6
  quedó diferida sin escribir código. Es la auditoría reflejando fielmente que la tool
  no se implementó, no un error de la auditoría.

Se comprobó que la auditoría **sí vigila lo nuevo**: se quitó `url` de `ContainerRegistry`
en `src/arcane-client.ts`, se ejecutó el script, apareció `FALTA-EN-TS-REQUERIDO` y el
total subió a 25; se revirtió con el fichero original y volvió a 24.

### 3.2 Tests e2e: 76 de 76 al cierre original, 77 de 77 en la revisión final

```bash
./scripts/e2e-remoto.sh > /tmp/e2e-cierre.txt 2>&1
grep -c "✓" /tmp/e2e-cierre.txt   # → 76, igual que el resumen "76 passed"
```

Se contaron las líneas `✓` una a una en vez de fiarse del resumen del reporter, porque un
fichero que aborta al importar sale como `skipped` y `skipped` no es verde. El conteo dio
**76**, exactamente el `76 passed` del resumen de vitest — ningún fichero abortó.

**1Password se autobloqueó a mitad del script**, en el primer `ssh` de la copia del árbol
de trabajo (`sign_and_send_pubkey: signing failed ... communication with agent failed`,
intento 1 de 10 fallido). El propio mecanismo de reintentos de `e2e-remoto.sh` —no una
intervención manual— resolvió el bloqueo en el segundo intento y la corrida completa
terminó limpia en **un solo intento del script** (39,79 s, 0 fallos de aserción). Se
declara aquí porque la instrucción del proyecto es parar y reportar ante cualquier fallo
de firma; en este caso el propio reintento se recuperó solo antes de que hiciera falta
intervenir, así que no hubo bloqueo real que reportar más allá de esta nota.

### 3.2.1 La corrida de la revisión final: 77 de 77

B3 añade un `it()` nuevo (`delete() borra el registro y list() deja de verlo`) a
`registries.e2e.ts`; B2 añade una aserción a un `it()` ya existente en `builds.e2e.ts`
(no suma marca `✓` propia). La corrida esperada por aritmética era 76 + 1 = 77.

```bash
./scripts/e2e-remoto.sh > /tmp/e2e-final2.txt 2>&1
grep -c "✓" /tmp/e2e-final2.txt   # → 77
grep -iE "✗|fail|skip" /tmp/e2e-final2.txt   # → ninguno
```

**Contado a mano: 77, exactamente lo esperado.** `Test Files 9 passed (9)`, `Tests 77
passed (77)`, 0 fallos, 0 skips, `resolvers.e2e.ts` completo (incluido el test del
último contenedor ordenado por nombre, el del defecto intermitente conocido — pasó
limpio esta vez, no hizo falta repetir la corrida por él).

**Esta revisión sí encontró un bloqueo real por 1Password**, distinto del autobloqueo
autorresuelto que documenta la §3.2: un intento anterior de esta misma sesión, lanzado
para demostrar por mutación que B2 y B3 muerden, se quedó sin firmar SSH en repetidos
`sign_and_send_pubkey: signing failed ... communication with agent failed` a través de
varios pasos y reintentos — sin autorrecuperarse. Siguiendo la instrucción del
proyecto, se paró la ejecución y se reportó al propietario en vez de forzar un rodeo;
el propietario desbloqueó 1Password y la corrida limpia de cierre —la que cuentan estas
cifras— se hizo **en un solo intento del script**, con un único reintento interno menor
(`Connection refused` transitorio en el primer `ssh` de `copia()`, resuelto en el
segundo intento de ese paso, sin relación con la firma).

### 3.3 Cobertura de operaciones método+ruta

El script AST se reescribió desde cero en `scripts/_tmp-audit-route-coverage.mjs` —
temporal, no se commitea, se borra después de medir— y se verificó que resuelve los tres
casos que rompieron versiones anteriores **antes** de fiarse de su número:

1. **`requestNdjson()` con el método como primer argumento.** El script busca las cinco
   variantes (`request`, `requestNdjson`, `requestSinCuerpo`, `requestMultipart`,
   `requestHead`), no solo `.request()`. Confirmado sobre `ProjectsMethods.start()` y
   `.pull()`: `POST /environments/{param}/projects/{param}/up` y `.../pull` aparecen.
2. **El ternario de `EventsMethods.list()`.** `base` tiene dos valores posibles según
   `opts?.environmentId`; el script expande ambas ramas del `ConditionalExpression` en
   vez de una. Confirmado: aparecen tanto `GET /events` como
   `GET /events/environment/{param}`.
3. **El recorte por el primer `?` literal en `ProjectAdditionalMethods.destroy()`.** La
   plantilla contiene `?removeFiles=${removeFiles ?? false}&removeVolumes=...`: un
   recorte ingenuo sobre el texto fuente crudo cortaría en el `??` de la expresión antes
   de llegar al `?` real de la query. Este script solo busca `?` en el texto **literal**
   de los quasis del template — nunca dentro de un `${...}` — así que el `??` nunca
   cuenta. Confirmado: aparece `DELETE /environments/{param}/projects/{param}/destroy`,
   sin el resto de la query.

Un cuarto caso, propio de F5: **`BuildWorkspaceMethods.ruta()`**, un helper privado que
construye la ruta *fuera* de la llamada a `request()`. El script inlinea llamadas
`this.<método>(...)` a otro método de la misma clase (sustituyendo parámetros,
profundidad acotada a 4) en vez de darlas por perdidas. **Se resolvió sin necesidad de
contar a mano:** las cuatro combinaciones de `BuildWorkspaceMethods` —`GET
.../builds/browse`, `GET .../builds/browse/content`, `POST .../builds/browse/mkdir`,
`DELETE .../builds/browse`— aparecen correctamente en el resultado.

El primer intento de ejecución **sí** encontró un fallo propio del script, no del
cliente: `requestHead()` usa el método HTTP `HEAD`, y una primera versión asumía `GET`
por comodidad de implementación, lo que hacía aparecer `GET
/environments/{param}/system/health` como «ausente en el spec» (el spec solo declara
`head` para esa ruta). Corregido antes de fiarse del número.

```
Combinaciones metodo+ruta unicas usadas por el cliente: 113
No resueltas estaticamente: 0
Ausentes en el spec: 0
Cobertura: 113 de 347 operaciones del spec.
```

El denominador honesto se reprodujo con el comando de
[`criterio-exposicion.md` §5](../arquitectura/criterio-exposicion.md), actualizado esta
fase: `347 − 51 swarm − 47 administración − 4 escrituras de registro de contenedor − 1
`builds/browse/download` redundante = 244`. Ninguna de las 113 combinaciones cae en las
zonas descontadas, así que la cobertura sobre el denominador honesto es también 113:
**113 de 244**.

### 3.3.1 Corrección de la revisión final: la comparación con F4 mezclaba dos instrumentos

El balance original comparaba «113 de 244» contra «98 de 249 al cierre de F4» y
presentaba el 98 como partida **verificada** al arrancar F5. **No se había remedido**:
era una copia del balance de F4, escrito con su propio script AST temporal — reescrito
desde cero en cada fase (F3, F4, F5 lo hacen así) y borrado sin commitear al terminar,
así que el script de F4 ya no existe y no se puede volver a correr.

**Se midieron los dos números con el mismo instrumento**, el script AST de F5
(`scripts/_tmp-audit-route-coverage.mjs`, recreado para esta revisión con el mismo
comportamiento descrito en la §3.3 — cinco variantes de `request*`, expansión de
ternarios, corte en `?` solo dentro de quasis literales, e inlining de
`BuildWorkspaceMethods.ruta()`), sobre el árbol de `e72bbcf` (cierre de F4) y sobre el
árbol de cierre de esta fase:

```
$ node scripts/_tmp-audit-route-coverage.mjs /tmp/arcane-client-e72bbcf.ts openapi.txt
Combinaciones metodo+ruta unicas usadas por el cliente: 97
No resueltas estaticamente: 0
Ausentes en el spec: 0
Cobertura: 97 de 347 operaciones del spec.

$ node scripts/_tmp-audit-route-coverage.mjs src/arcane-client.ts openapi.txt
Combinaciones metodo+ruta unicas usadas por el cliente: 113
No resueltas estaticamente: 0
Ausentes en el spec: 0
Cobertura: 113 de 347 operaciones del spec.
```

(`arcane-client.ts` en `e72bbcf` se extrajo con `git show e72bbcf:src/arcane-client.ts`;
`openapi.txt` es bit a bit idéntico en los dos commits, verificado con `diff`.)

**97, no 98.** La diferencia de uno es
`VolumeBackupsMethods.download()` (`src/arcane-client.ts`, sin tocar desde antes de
F4): en vez de pasar por `request()` como los demás métodos, llama a
`this.client.fetchFn(...)` directamente con la URL construida a mano —

```ts
const response = await this.client.fetchFn(
  `${this.client.getBaseUrl()}/environments/${encodeURIComponent(envId)}/volumes/backups/${encodeURIComponent(backupId)}/download`,
  { method: "GET", headers: { "X-API-Key": this.client.getApiKey() } },
);
```

— y el script de F5 solo busca las cinco variantes `request`/`requestNdjson`/
`requestSinCuerpo`/`requestMultipart`/`requestHead`, tal y como dice su propio nombre.
`GET /environments/{id}/volumes/backups/{backupId}/download` es una ruta real, cubierta
por el cliente, en los dos commits por igual (`download()` no cambió entre `e72bbcf` y
hoy, verificado con `git diff` — F5 solo le tocó el manejo de errores, cambiándolo a
`lanzaSiFalla()`). El script no la ve en ninguno de los dos extremos: es un límite del
instrumento, no del código, y afecta a la partida y a la llegada por igual.

**Por eso el avance real es +16, no +15.** Comparar 113 (script de F5, sin ver
`download()`) contra 98 (instrumento de F4, que sí la veía de algún modo, ya
irreproducible) resta dos números de instrumentos distintos: 113 − 98 = 15, pero ese 15
descuenta la ruta de `download()` una sola vez en vez de cero o dos. Comparar 113 contra
97 —el mismo instrumento en los dos extremos, ambos sin ver `download()` por igual— da
113 − 97 = **16**, la cifra que sí representa cuántas combinaciones método+ruta nuevas
entraron en F5. La cobertura absoluta publicada como cifra de cierre de fase sigue
siendo la medida directamente, **113 de 244**; lo que cambia es la cifra de *partida*
(pasa a publicarse como 97, no 98) y el avance derivado de ella (+16, no +15).

## 4. Lo que apareció y no estaba en el plan

A diferencia de F4, donde los hallazgos no planeados eran sobre todo del código
desplegado (una inyección de ruta, dos tools rotas en producción), en F5 la mayoría
**vienen del propio plan**, cazados por los implementadores y por la revisión de cada
tarea, no por el plan mismo.

### 4.1 Una afirmación falsa sobre el spec: `{kind}` sí está enumerado

El plan y el spec de F5 afirmaban que `openapi.txt` no enumeraba los valores posibles de
`{kind}` en las rutas de upload/download. **Era falso.** El spec sí los declara:
`["image", "volume-backup", "build-workspace"]`. Y el valor correcto para el flujo de
subida del workspace de builds es `build-workspace`, no `builds` — el nombre que el
borrador había asumido por analogía con la ruta `/builds/browse`. Lo cazó el
implementador de la Tarea 6 mientras investigaba por qué el flujo de subida no
verificaba en ningún entorno.

### 4.2 Un código de ejemplo que contradecía tres veces sus propias restricciones

El código de ejemplo de una de las tareas violaba, él mismo, tres restricciones globales
que el propio plan impone a todas las tareas:

1. **Cubría menos parámetros opcionales de los que el método acepta.** La restricción 3
   exige dos tests por cada parámetro opcional del método, no solo los que aparezcan en
   el ejemplo — ya reforzada explícitamente tras la Tarea 3, que sufrió el mismo defecto
   en F4. El ejemplo de F5 volvió a caer en él.
2. **No incluía ningún test de cableado de handler.** La restricción 3b —añadida
   precisamente por este motivo en la Tarea 3— exige que cada handler de tool tenga al
   menos un test que compruebe que pasa sus parámetros al cliente. El ejemplo no traía
   ninguno.
3. **Usaba `.callback` para acceder al handler de una tool en los tests, donde el SDK de
   MCP expone `.handler`.** Detectado por el implementador de la Tarea 5 y corregido en
   el plan (commit `3accf99`) en sus ocho apariciones, antes de que las Tareas 7 y 8 lo
   heredaran.

Ninguno de los tres lo introdujo un implementador: los tres estaban ya en el texto que
se les entregó.

### 4.3 Una mutación de falsabilidad que no falsificaba el código que el plan proponía

La restricción 5 exige mutar el código de producción y demostrar que el test cae, antes
de dar una tarea por terminada. Una de las mutaciones que el propio plan prescribía para
`ImageBuildsMethods` —cambiar `success: done` por `success: true`— **no rompía ningún
test del código de ejemplo**, porque la rama que maneja `{error}` retorna antes de
llegar a esa línea: nada en el ejemplo ejercitaba el caso «sin error y sin `done:true`».
El implementador de la Tarea 7 añadió ese test; el revisor verificó la falsabilidad
borrándolo y repitiendo la mutación para confirmar que, sin el test nuevo, la mutación
era invisible.

### 4.4 Una descripción que hablaba de un parámetro que el esquema no aceptaba

La descripción de `arcane_image_build` en el plan mencionaba `load:false` como parte del
comportamiento de la tool, pero el esquema propuesto para esa misma tool no aceptaba
`load` como parámetro. El implementador de la Tarea 8 lo resolvió añadiendo `load`
—junto con `labels`, `platforms` y `provider`— al esquema real, en vez de dejar la
descripción hablando de algo que la tool no podía recibir.

### 4.5 Otros dos datos medidos que no estaban en el plan

- **`UploadSession.receivedChunks` es un array, no un número.** El plan lo daba por
  numérico sin haberlo medido contra la instancia.
- **El 404 que Zabbix devuelve en `/uploads/build-workspace` revela que el agente
  remoto reescribe el id de entorno al suyo local (`0`)** — el mismo comportamiento ya
  conocido de `ImageBuildRecord.environmentId`, ahora confirmado también en esta ruta.

## 5. Predicciones del plan que resultaron falsas

A diferencia de F4 —donde el plan predijo tres cifras de cierre y las tres eran
falsas—, en F5 las dos cifras de cierre que el brief de la Tarea 9 predijo con un
número concreto **se cumplieron exactamente al medirlas**: el denominador honesto dio
`244` (predicción: `244`) y `BuildWorkspaceEntry` apareció en el drift con
`relativePath` y `editable` marcados, tal como se esperaba.

La única cifra de alcance que cambió respecto al objetivo original del plan («exponer
17 tools») es la **17.ª tool, `arcane_build_workspace_upload`, que no se entregó**: no
por una predicción equivocada, sino por una decisión del propietario documentada en la
Tarea 6, después de medir que ningún entorno soporta las tres llamadas encadenadas que
el flujo de subida necesita. F5 cierra con 16.

Las afirmaciones falsas de esta fase —§4.1 y §4.5— no eran predicciones de cifras de
cierre: eran datos que el plan daba por sentados sin haberlos medido contra la instancia,
y que resultaron incorrectos al medirlos durante la implementación.

## 6. Qué queda pendiente

### Cerrado por la revisión final de rama

La revisión final de rama sobre F5 no encontró ningún Critical: la superficie de rutas
está limpia y el enmascarado de `buildArgs` está bien puesto. Lo que encontró era sobre
todo texto que lee el modelo, documentación con cifras sin remedir, y dos huecos de
test. De los pendientes que las tareas 1-8 habían dejado señalados, esta revisión
cerró:

- **`template-registries.ts`:** la descripción de `_create` omitía `enabled`, el cuarto
  campo obligatorio del DTO — corregido. La de `_update` no avisaba de que hay que leer
  los valores actuales con `_list` antes de escribir los cuatro campos (si no, un
  `enabled`-only intent inventa `url` y la reescribe en silencio) — añadido el aviso.
- **`image-builds.ts`:** `arcane_image_build_get` no repetía el aviso de que
  `environmentId` es el id local del agente remoto, aviso que sí llevaba `_list` —
  añadido. `_get` volcaba `output` entero vía `JSON.stringify` sin cota propia,
  dependiendo de un límite del servidor nunca medido — se le aplicó el mismo recorte
  por la cola que usa `summarizeBuildStream()`, reutilizando
  `LINEAS_DE_LOG_CONSERVADAS`, con prosa de cuántas líneas se omitieron y test
  demostrado por mutación.
- **`container-registries.ts`:** `arcane_container_registry_test` afirmaba «Does not
  change any state» sobre un POST que hace un `registry login` real contra un tercero
  con el camino de éxito no visto — reescrito para decir solo lo medido: no modifica la
  configuración del registro en Arcane.
- **`build-workspace.ts`:** las cuatro tools callaban que, medido hoy, solo 1 de 6
  entornos tiene el workspace utilizable — añadido a las cuatro descripciones, sin
  inventar el motivo. `arcane_build_workspace_read` reportaba `bytes.length` como si
  fuera el tamaño del fichero, que deja de ser cierto en cuanto `maxBytes` recorta la
  lectura — reescrito para decir «bytes read» y avisar cuando la lectura pudo quedar
  truncada.
- **Falta un test que el spec prometió (§5.2):** que el listado de builds no trae
  `output` hoy. Añadida la aserción en `builds.e2e.ts`.
- **`registries.e2e.ts`:** `arcane_template_registry_delete` no tenía e2e asertivo, solo
  se ejercitaba en el `afterAll` dentro de un `try/catch` que solo logueaba. Añadido el
  test asertivo que cierra la cadena crear → listar → actualizar → borrar (spec §7.2b),
  con el `afterAll` intacto como red de seguridad.
- **Las dos cifras de C1/C2** (comparación de cobertura con instrumentos distintos,
  rango de commits que empezaba en el commit equivocado) — ver §2 y §3.3.1.
- **`docs/arquitectura/criterio-exposicion.md`** no cubría las cuatro escrituras de
  `templates/registries` y su §1 seguía diciendo «nunca las escrituras» sin matiz —
  añadida la §2.6 con la justificación (ya estaba en el spec §2.3, no había llegado al
  criterio) y matizada la §1.
- **`docs/desarrollo/anadir-una-tool.md`** decía «e2e contra la instancia v2.7.0 real»;
  es 2.8.0 — corregido.
- **El spec de F5** decía en dos sitios «17 tools» / «`build-workspace.ts` 5 tools»,
  obsoleto desde que se difirió `upload` — corregidos ambos.

### Heredado de tareas anteriores, sin arreglar (fuera del alcance de esta revisión)

- **Tarea 1** (`scripts/e2e-remoto.sh`): el `chmod 600` final de `copia()` no lleva
  `|| return 1` — si falla, reintenta la copia entera del árbol en vez de solo ese paso.
  Ineficiente, no incorrecto. Ninguna llamada `ssh` usa `BatchMode=yes`.
- **Tarea 4** (`registries.e2e.ts`): `list()`/`update()` siguen encadenados al id que
  asigna `create()` en vez de usar un `beforeAll`, así que si `create()` falla los
  siguientes fallan con mensajes confusos (sin residuo, solo mal diagnosticado). La
  constante `CAMPOS` en `template-registries.ts` sigue rompiendo la convención
  `LIST_PARAMS` del resto de `src/tools/*.ts`. Ninguno de los dos era parte de los
  hallazgos de la revisión final.
- **Tarea 7** (`image-builds.ts`): `summarizeBuildStream()` sigue sin test para entradas
  degeneradas (stream vacío, log no-string, error vacío): el comportamiento es
  coherente pero no está demostrado. `BuildRequest` sigue sin `cacheFrom`, `cacheTo`,
  `entitlements`, `extraHosts`, `isolation`, `network`, `privileged`, `shmSize` ni
  `ulimits` — **la revisión final reclasifica esto de deuda a decisión deliberada**:
  esos campos son precisamente la superficie que este fork no quiere en manos de un
  modelo (control fino de aislamiento del build, límites de memoria compartida,
  ulimits del proceso), no un olvido a rellenar en una fase futura.
- **Tarea 8** (`image-builds.ts` / `build-workspace.ts`): ninguna descripción de tool
  menciona que los endpoints de build devuelven HTTP 200 aunque la build falle — el
  código lo compensa y está probado, pero el hecho medido no está en el texto que ve el
  modelo. No era parte de los hallazgos de la revisión final; queda para la próxima.
- **El arreglo `12376d6` de la Tarea 7 SÍ pasó, en esta revisión, por ojos frescos**: la
  revisión final de rama lo repasó junto con el resto de `image-builds.ts` y no generó
  hallazgo propio sobre él.

### Medido en esta revisión final, para que conste

- **`arcane_build_workspace_read` se entrega sin e2e.** No es un olvido: con `upload`
  diferida (Tarea 6, §3.1 del spec) no hay forma de meter un fichero en el workspace de
  builds por la API, así que `read` no se puede ejercitar contra la instancia — el mismo
  criterio con el que se difirió `upload`, aplicado aquí a una tool y no a la otra. La
  tool no se toca ni se disfraza: se declara. Ver también «Zonas no vistas» más abajo.
- **El schema `EnvironmentEnvironment` del spec de Arcane declara una propiedad
  `apiKey`,** y el tipo `Environment` de este fork la anticipa (campo opcional, sin
  usar). Medido hoy contra la instancia real: **NO se devuelve**, ni en
  `GET /environments` ni en `GET /environments/0` — las claves reales del objeto son
  `apiUrl, enabled, id, isEdge, lastSeen, name, status`. `arcane_environment_list` y
  `_get` vuelcan el objeto crudo, así que hoy no hay fuga: si algún día la API empieza a
  incluir `apiKey`, este es el sitio a revisar primero. Conviene remedirlo al saltar de
  versión de Arcane.

### Defectos conocidos, sin tocar por decisión explícita

- **`arcane_volume_backup_download` es hoy una tool falsa en producción.** No llama al
  cliente en ningún caso: devuelve una cadena de texto fija
  (`src/tools/volume-backups.ts`, handler de `arcane_volume_backup_download`) con
  instrucciones para usar la API directamente. No pertenece a F5 y no se ha tocado, pero
  queda declarado porque cualquier balance que no lo mencione estaría dejando pasar una
  tool que miente sobre lo que hace.
- **El defecto intermitente de `resolvers.e2e.ts`.** Falla ~2 de cada 12 corridas, con
  la hipótesis de un contenedor Trivy efímero interfiriendo en el orden de contenedores
  que consulta `resolveContainerId`. **La hipótesis no está demostrada** — es la
  explicación más plausible medida hasta ahora, no una causa confirmada. No se ha
  tocado, como pide la restricción del proyecto de no inventar la causa de un aviso sin
  medirla primero.
- **`arcane-client.ts` pesa 91.398 bytes (≈89,3 KiB), por encima de los 77 KB desde los
  que este proyecto viene pidiendo partición.** Sigue siendo un único fichero con quince
  clases `*Methods`; F5 añadió tres más (`ContainerRegistriesMethods`,
  `TemplateRegistriesMethods`, `BuildWorkspaceMethods`, `ImageBuildsMethods`) sin
  dividirlo. Sin acción en F5.
- **Los listados de `container-registries` y de `images/builds` devuelven
  `grandTotalItems: 0` con `totalItems` distinto de cero.** Medido hoy mismo: un
  registro de contenedor sembrado y borrado en el momento (`totalItems=1,
  grandTotalItems=0`), y el historial real de builds (`totalItems=125,
  grandTotalItems=0`). Incoherente y medido, candidato a un segundo issue upstream junto
  al de `hasBuildDirective`. Sin acción en F5.

### Zonas no vistas

- **El camino de éxito de `container_registry_test`.** No se puede ejercitar sin
  credenciales reales de un registro externo; esta instancia no las tiene. Solo se ha
  visto el camino de fallo (host inexistente, error que nombra el host).
- **El workspace de builds con jerarquías profundas.** Solo se ha visto vacío y con un
  directorio de primer nivel; no se ha medido qué devuelven `browse`/`read` con árboles
  anidados.
- **`arcane_build_workspace_read` en su conjunto.** Sin `upload` no hay forma de meter
  un fichero de prueba en el workspace por la API, así que la tool se entrega sin e2e —
  ver el detalle arriba, en «Medido en esta revisión final».

## 7. Referencias

- [Spec de F5](../superpowers/specs/2026-08-19-f5-build-y-registries-design.md)
- [Plan de F5](../superpowers/plans/2026-08-19-f5-build-y-registries.md)
- [Criterio de exposición — denominador honesto, actualizado a 244](../arquitectura/criterio-exposicion.md)
- [Auditoría: `hasBuildDirective` siempre `false` en el endpoint que un cliente usaría](../auditorias/2026-08-19-has-build-directive-upstream.md)
- [Balance de F4](2026-08-19-f4.md)
- [Registro completo de la sesión](../../.superpowers/sdd/progress.md)
