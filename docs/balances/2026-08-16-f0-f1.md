# Balance de F0 (cimientos) y F1 (visibilidad)

- **Fecha:** 2026-08-16
- **Punto de partida:** `eb866b6` — rama del fix NDJSON publicada pero **fuera de `main`**
- **Punto de llegada:** `bea2969` — 32 commits, 25 ficheros, +4.269 / −231
- **Estado:** F0 y F1 completadas, desplegadas y verificadas contra la instancia real

---

## 1. Resumen en una frase

El fork pasó de tener cinco tools rotas en producción, una suite de tests que no
arrancaba y un spec de API tres versiones desfasado, a estar verificado campo a campo
contra Arcane v2.7.0 con 105 tests, cero desalineaciones graves y un PR público abierto
en el upstream.

## 2. Estado verificable hoy

| Métrica | Antes | Ahora |
|---|---|---|
| `npm test` | **no arrancaba** | 105 tests en 3 ficheros, en verde |
| `openapi.txt` | v1.14.1 — 140 paths | **2.7.0** — 268 paths, 628 schemas |
| Drift de campos (graves) | sin medir | **0** (de 55 detectados) |
| Drift restante | — | 22 `FALTA-EN-TS-OPCIONAL`, diferidos a F2–F5 |
| Cobertura de rutas | "37/37" (cifra heredada, errónea) | **58/58** medidas |
| Tools documentadas en el README | 38 de 68 | **68 de 68**, generadas del código |
| Tools rotas conocidas | 3 (`stack_start`, `project_redeploy`, `stack_pull`) + 2 (`template_create/update`) | **0** |

Comandos que ahora existen y antes no:

```bash
npm run test:e2e          # verificación contra la instancia Arcane real
npm run update-api-spec   # refresca openapi.txt desde la instancia
npm run gen-tools-table   # regenera la tabla del README desde src/tools/
node scripts/audit-schema-drift.mjs   # audita el drift de campos
```

## 3. Lo planificado, y lo que apareció por el camino

El plan tenía 9 tareas. Se ejecutaron 11, y **los cuatro hallazgos de mayor valor no
estaban en ninguna de ellas**.

### Lo planificado (F0)

| Fase | Entregable |
|---|---|
| F0.1 | Fix NDJSON de `/up` y `/redeploy` llevado a producción y verificado |
| F0.2 | `openapi.txt` a v2.7.0 con `scripts/update-api-spec.mjs` |
| F0.3 | Auditoría de drift automatizada + corrección de las interfaces |
| F0.4 | Runner de tests reparado |
| F0.5 | `docs/desarrollo/anadir-una-tool.md`, el patrón para F2–F5 |

### Lo planificado (F1)

| Fase | Entregable |
|---|---|
| F1.1 | README de fork mantenido, con las divergencias respecto al upstream |
| F1.2 | PR quirúrgico al upstream: [cougz/arcane-mcp-server#1](https://github.com/cougz/arcane-mcp-server/pull/1) |
| F1.3 | Publicación en GitHub (ya estaba hecha antes de la sesión) |

### Lo que apareció y no estaba previsto

**1. La premisa de despliegue del proyecto era falsa.**
El spec y el plan afirmaban que un push a `main` reconstruye y reinicia producción.
No es cierto. GitOps sincroniza los ficheros al host **e intenta redesplegar**, pero
ese redeploy falla al resolver la imagen base:

```
redeploy failed: failed to prepare project images for deploy: node:22-bookworm-slim:
failed to resolve source metadata for docker.io/library/node:22-bookworm-slim:
no active sessions
```

No es un problema de red — el registro responde desde el host —, sino que el build que
lanza Arcane no dispone de sesión de BuildKit. **Consecuencia:** desplegar exige un
`docker compose up -d --build` manual, y un `lastSyncStatus` en `failed` es el estado
habitual de este proyecto, no una alarma.

El primer despliegue de la sesión fue un **despliegue fantasma**: sync en verde, con
código de dos meses antes sirviendo. Se detectó comparando `requestNdjson` dentro del
contenedor (5 ocurrencias) contra el disco sincronizado (8).

**2. `arcane_template_create` y `arcane_template_update` eran inservibles.**
Enviaban `composeContent` cuando el campo real es `content`, más `category` y `tags`
—que no existen en la API v2—, y trataban `description` y `envContent` como opcionales
pese a ser obligatorios. Es la misma familia de fallo que descartó al proyecto
alternativo evaluado en el spec, viva dentro de nuestro propio fork.

**No se habría detectado con el alcance original**: el mapa de la auditoría cubría 9
interfaces y `Template` no estaba entre ellas.

**3. `arcane_stack_pull` reportaba fallo en pulls correctos.**
El criterio de éxito exigía un evento centinela `{"status":"complete"}` que Arcane no
emite. Verificado en vivo:

| | |
|---|---|
| Antes | `Error: Pull failed for stack 'ical-bridge': Pull finished (2 events)` |
| Después | `Images pulled successfully for stack 'ical-bridge' in environment '0'. Pull finished (2 events)` |

Los mismos dos eventos, veredicto opuesto. Salió al revisar el PR al upstream, y
estaba en producción, no solo en el PR. Generó la Tarea 10, fuera de plan.

**4. El arreglo anterior estaba a medias, y solo se supo al capturar el stream real.**
La Tarea 10 dejó `arcane_stack_pull` devolviendo `Pull finished (2 events)`. Se asumió
que era el mensaje que daba de sí ese stream. Al capturarlo con `curl` resultó que no:
`/pull` usa **el mismo sobre** que `/up` y `/redeploy`.

Con imagen local, sin registro (`ical-bridge`):

```
{"type":"activity","activityId":"bf40d2c4-53a0-4058-9b1b-296442fdf6b1"}
{"done":true}
```

Con imagen de registro (`gitea/gitea:1.25.5`):

```
{"type":"activity","activityId":"77c852a9-af6e-4e05-9180-1e021b22c8b6"}
{"log":"1.25.5: Pulling from gitea/gitea"}
{"log":"Digest: sha256:f846d26a4fc389c5806a580a765e00bfdd1fd181e6f2060da98ea2669d914472"}
{"log":"Status: Image is up to date for gitea/gitea:1.25.5"}
{"done":true}
```

Claves observadas en todo el stream: `activityId`, `done`, `log`, `type`. **Nunca
`status` ni `id`** — que es justo el campo del que `summarizePullStream()` construía el
mensaje. Por eso degradaba siempre al conteo: estábamos descartando la salida real de
docker, que sí llegaba. Y `{"done":true}` ofrecía una confirmación **positiva** de éxito
que tampoco se usaba.

Los cuatro endpoints NDJSON pasan a compartir `summarizeComposeStream()`, con la
detección de errores unificada en `extractStreamError()` (`error` + `errorDetail`). El
código pierde 235 líneas y gana 139. Verificado en vivo:

| Stack | Imagen | Resultado |
|---|---|---|
| `gitea` | de registro | `1.25.5: Pulling from gitea/gitea \| Digest: sha256:f846d26a… \| Status: Image is up to date…` |
| `ical-bridge` | `:local` | `Pull finished (2 events)` — correcto: docker salta la imagen y el stream no trae `log` |

Generó la Tarea 11, también fuera de plan. La lección se repite: la Tarea 10 corrigió el
síntoma (el veredicto) sin haber observado nunca el stream; la Tarea 11 lo observó y
corrigió la causa.

## 4. La auditoría de drift, en detalle

El fallo que se quería evitar era de **campos, no de rutas**. Los paths ya estaban
validados; los shapes no.

Medición inicial con 9 interfaces: 66 hallazgos, 27 graves.
Tras ampliar a **14 interfaces de payload**: **118 hallazgos, 55 graves**.

Los peores casos, todos corregidos:

- **`NetworkSummary`** declaraba `internal`, `attachable`, `ingress` e `ipam`.
  **Ninguno existe** en v2.7.0. Le faltaban `inUse`, `isDefault`, `labels` y `options`,
  los cuatro obligatorios.
- **`Volume`** no declaraba `id`, `inUse`, `scope`, `containers`, `labels` ni `options`
  — los seis obligatorios.
- **`GitOpsSync`** declaraba un `status` inexistente y le faltaban 8 campos obligatorios.
- **`Template`**: ver arriba.

Confirmado contra datos vivos de la instancia: `NetworkSummary` real devuelve
exactamente `id, name, driver, scope, created, options, labels, inUse, isDefault`, y
`Volume` real trae los seis obligatorios que la interfaz no declaraba.

Quedan 22 `FALTA-EN-TS-OPCIONAL` diferidos a propósito, todos de dominios que ninguna
tool consume todavía (`updateInfo`, `vulnerabilityScan`, `projectFiles`, los `preDeploy*`
de GitOps…). Están listados en
[la auditoría](../auditorias/2026-08-16-drift-campos-v2.7.0.md).

## 5. Datos heredados que resultaron ser falsos

Tres cifras y una premisa que se daban por buenas y no lo eran. Las cuatro venían de
documentación previa, no de una medición:

| Afirmación heredada | Realidad medida |
|---|---|
| "Un push a `main` despliega" | GitOps sincroniza, intenta redesplegar y **falla** |
| "37/37 rutas válidas" | **58/58** — el cliente usa 58 combinaciones método+ruta |
| "F0.4 exige fijar `vitest` a `~3.2`" | Basta eliminar el pool de Workers: ningún test necesita runtime de Workers |
| "El runner falla por peer ranges incompatibles" | Los peers se satisfacen; vitest 4 no acepta `pool: "<paquete>"` |

La última se corrigió **antes de publicar el PR**, porque iba firmada en un repositorio
ajeno y era rebatible.

## 6. Qué queda pendiente

### Antes de empezar F2

De las tres que había al escribir este balance, **dos se cerraron el mismo día**:

- ✅ **El harness e2e se ejecutó por primera vez contra la API real:** 3/3 en verde. La
  credencial vive en `.dev.vars`, que está en la línea 1 del `.gitignore`. Con esto la
  regla dura del proyecto —test unitario **y** comprobación contra la instancia real— es
  ejecutable de verdad.
- ✅ **La forma del stream de `/pull` está capturada**, y el defecto que destapó,
  corregido y verificado (ver el punto 4 de la sección 3).

Quedaba una, **cerrada también el mismo día**:

1. ✅ **El redeploy de GitOps ya funciona.** No era un problema de configuración del
   builder ni de la imagen base: era un bug del módulo `go.getarcane.app/builds` v0.3.0
   que usa Arcane, con la sesión de BuildKit cerrándose antes de resolver los metadatos
   de la imagen base. Se corrigió actualizando Arcane a **v2.8.0** (módulo v0.3.1).
   El despliegue automático está restaurado y verificado con la imagen base ausente del
   host. La hipótesis que se apuntaba aquí —"que la imagen base esté disponible
   localmente"— habría funcionado por el motivo equivocado, enmascarando el bug.
   Coste: 2.8.0 elimina 9 operaciones, 2 de ellas en uso, que hubo que migrar.
   Ver [la auditoría de la actualización](../auditorias/2026-08-16-actualizacion-arcane-2.8.0.md).

### Infraestructura

2. **Disco del host `VM-Control`: resuelto.** Pasó de 98 GB al 90 % de uso a **295 GB al
   12 %**, con 251 GB libres, tras ampliarlo. Antes se liberaron 5,91 GB con
   `docker builder prune`. La limpieza de imágenes que quedaba pendiente pasa de
   necesaria a opcional.

### Fases siguientes

Cada una con su propio spec, según el diseño original: F2 System + Events/Jobs (~16
tools), F3 actualizaciones de imágenes (~9), F4 vulnerability scanning (~12), F5 build
+ registries (~14).

## 7. Qué funcionó, como método

Vale la pena registrarlo porque explica de dónde salieron los hallazgos.

**Revisar contra la fuente, no contra el plan.** Cada hallazgo grave de la sesión salió
de que alguien —un revisor, o la verificación en vivo— se negó a aceptar un dato
heredado. El plan decía 9 interfaces; el revisor contó los payloads reales. El plan
daba por bueno el centinela de `/pull`; el revisor pidió la evidencia de haberlo
observado, que no existía.

**Verificar en la capa correcta.** El despliegue fantasma se detectó mirando dentro del
contenedor. El estado del sync, los logs y el `docker ps` decían todos que estaba bien.

**Automatizar la comprobación en vez de la documentación.** La auditoría de drift y la
tabla de tools no son documentos mantenidos a mano: son scripts que leen el código con
el compilador de TypeScript. `gen-tools-table --check` falla con exit 1 si el README se
desactualiza. La tabla ya se había desincronizado una vez precisamente por mantenerse a
mano.

**Dos trampas de método que costaron tiempo**, anotadas para no repetirlas:

- `pgrep -f` se auto-matchea si el patrón aparece en el propio comando. Produjo un
  diagnóstico falso ("hay builds constantemente") que solo se deshizo al contrastar con
  `ps`. El truco de los corchetes (`pgrep -f "[b]uildx bake"`) lo evita.
- La primera llamada MCP tras cada redespliegue suele agotar el timeout y funcionar a la
  segunda. No es un fallo de la tool.

---

## Referencias

- [Spec de diseño F0+F1](../superpowers/specs/2026-08-16-fork-arcane-mcp-f0-f1-design.md)
- [Plan de implementación](../superpowers/plans/2026-08-16-fork-arcane-mcp-f0-f1.md)
- [Auditoría de drift de campos](../auditorias/2026-08-16-drift-campos-v2.7.0.md)
- [Cómo añadir una tool](../desarrollo/anadir-una-tool.md)
- [PR al upstream](https://github.com/cougz/arcane-mcp-server/pull/1)
